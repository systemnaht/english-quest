plugins {
    id 'com.android.application'
}

android {
    namespace 'de.ciaoenglish.app'
    compileSdk 35

    defaultConfig {
        applicationId 'de.ciaoenglish.app'
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName '0.1.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
